{
  "installCommand": "cd admin && npm install --legacy-peer-deps",
  "buildCommand": "cd admin && npm run build",
  "outputDirectory": "admin/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
